const express = require('express');
const {Catlog} = require('../models/catlogs');
const router = express.Router();

router.get('/seller/')

// router.post('/seller/create-catlog', async (req, res)=>{

// })

module.exports = router;